
model User {
      id String @id @default(uuid())
      email String @unique
      name String?
      role UserRole
      status UserStatus @default(ACTIVE)
      isDeleted Boolean @default(false)

      @@map("users")
      
}